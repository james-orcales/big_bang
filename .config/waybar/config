{
    "id": "WAYBAR_ARBITRARY_69",
    "ipc": true,
    "position": "bottom",
    "margin-top": 10,
    "margin-left": 10,
    "margin-right": 10,
    "margin-bottom": 10,
    "spacing": 5,

    "modules-left": ["clock#date"], 
    "modules-center": ["sway/workspaces"],
    "modules-right": ["memory", "pulseaudio#2", "pulseaudio", "network"],
    "sway/window": {
        "max-length": 50
    },

    "clock#date": {
        "format": "{:%b %d %a %H:%M}",
        "tooltip-format": "{:%c}"
    },
    "wlr/taskbar": {
        "sort-by-app-id": true,
        "format": "{icon}",
        "icon-size": 15,
        "icon-theme": "Papirus",
        "tooltip-format": "{title}",
        "on-click": "activate",
        "on-click-middle": "close",
    },

    "memory": {
        "interval": 30,
        "format": "{used:0.1f}G/{total:0.1f}G "
    },
    "pulseaudio": {
        "format": "{volume}% {icon}",
        "format-bluetooth": "{volume}% {icon}",
        "format-muted": "MUTE {icon}",
        "format-icons": {
            "headphone": "",
            "default": ["", ""]
        },
        "scroll-step": 10,
        "on-click": "pavucontrol"
    },
    "pulseaudio#2": {
        "format": "{format_source}",
        "format-source-muted": "",
        "on-click": "pavucontrol -t 4"
    },
    "network": {
        "format": "{ifname}",
        "format-wifi": "",
        "format-ethernet": "󰊗",
        "format-disconnected": "OFFLINE", //An empty format will hide the module.
        "tooltip-format": "{ifname} via {gwaddr} 󰊗",
        "tooltip-format-wifi": "{essid} ",
        "tooltip-format-ethernet": "{ifname} ",
        "tooltip-format-disconnected": "Disconnected",
        "max-length": 50
    }
}
